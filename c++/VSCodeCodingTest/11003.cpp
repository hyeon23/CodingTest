#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, L, tmp;
    cin >> N >> L;
    deque<int> deq;
    for(int i = 0; i < N; ++i){
        cin >> tmp;
        deq.push_back(tmp);
    }
}