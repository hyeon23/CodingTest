#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, L, tmp;
    cin >> N >> L;
    deque<int> dq;
    deque<int> dq2;
    for(int i = 0; i < N; ++i){
        cin >> tmp;
    }

    int f = dq.front();
    dq.pop_front();

    while(!dq.empty()){
        
        
    }
}