#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
    int N, tmp, m = 10001;
    cin >> N;
    vector<int> vec(N);

    for(int i = 0; i < N; ++i)
        cin >> vec[i];

    for(int i = 1; i <= N; ++i){//1개 ~ N개
        for(int j = 0; j < i; ++j){
            for(int k = 0; k < N; ++k){
                if(j != k){
                    
                }
            }
        }
    }
}