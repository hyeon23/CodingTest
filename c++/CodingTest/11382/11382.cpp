#include <iostream>
#include <stdio.h>

using namespace std;
int main() {
	//sol1
	long long A, B, C;
	cin >> A >> B >> C;
	cout << A + B + C;

	//sol2
	/*scanf("%lld %lld %lld", &a, &b, &c);
	printf("%lld", a + b + c);*/

	return 0;
}